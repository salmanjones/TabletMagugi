import {StyleSheet} from 'react-native';
import {PixelUtil} from '../utils';

export const timerRightWidgetStyles = StyleSheet.create({
    timeTxt: {
        color: '#ffffff',
        fontSize: PixelUtil.size(30)
    }
});
